# Pelorus governance hooks. Installation is explicit: make hooks-install.
pre-commit:
  parallel: false
  commands:
    context-check:
      run: make compile-context-verify
    hiss-audit:
      run: make audit

pre-push:
  parallel: false
  commands:
    verify-all:
      run: make verify-all
