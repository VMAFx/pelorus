version: 2
updates:
  # C/Vulkan has no dependabot-managed package ecosystem here; keep the GitHub
  # Actions used by CI / release pinned and current.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
    labels: [dependencies, ci]
