- **`vf_pelorus_mc_vulkan` sub-pel motion vectors**: the integer block-match minimum is now refined to sub-pel by a parabolic fit of the SAD surface across the minimum and its four axis-neighbours (four extra cooperative `block_sad` reductions, no new fetches), and the MV grid is emitted in **quarter-pel** Q2 fixed-point (`stored = round(pel*4)`) — conforming the `PelorusMotionSection` MV field to ADR-0113's long-specified ¼-pel contract. The summary scalars (`global_motion_*`, `motion_magnitude_*`) stay in whole luma pixels (host derivation divides the grid by 4). The NVENC external-ME-hint consumer (patch 0008) round-divides the quarter-pel grid by 4 into its integer-pixel hint field, which also corrects the prior 4×-too-small hint. No ABI bump (`PelorusMotionSection` layout unchanged); `pelorus_mc.comp` updated in lockstep. Validated on a known ~0.51 px/frame scroll: measured global motion `≈ 0.55` px (fractional) where integer-pel mc rounded to 0/1. This is the prerequisite for the ADR-0113 MC→denoise motion-compensated warp (the denoise consumer reads the grid with `mv_scale = 0.25`). ADR-0130.
