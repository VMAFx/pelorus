- **`vf_pelorus_analyze` per-frame complexity scalar (`PEL_SEC_COMPLEXITY`, ABI minor 3)** — ADR-0132 step 1, the producer the per-shot CRF steering and the autotune loop consume. analyze aggregates the texture/edge energy it already measures into a normalized `complexity ∈ [0,1]` (folding in `motion_component` when an upstream `pelorus_mc` attached `PEL_SEC_MOTION`), EMA-smoothed across frames and reset on a scene cut, emitted append-only as a new 16-byte section. `PelorusMotionSection` and every prior section are untouched; the conformance fixture round-trips it + asserts R4. Validated to track content on the RTX 4090: flat 0.000 < textured 0.037 < high-motion BBB 0.164. No encoder steering yet (pure producer, no regression risk) — the qoffset apply + autotune-learned mapping are the sequenced ADR-0132 follow-ons.
