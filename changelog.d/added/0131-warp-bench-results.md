- **MC→denoise warp BD-rate proof + RD-curve graphs**: `docs/development/bench-results.md` v0.3 documents the ADR-0131 warp on a genuinely high-motion source (Big Buck Bunny `00:09:00`, ~15 px/frame — the shipped 640×360 corpus clip was measured low-motion and unsuitable). On the RTX 4090 with `hevc_nvenc`, pre-encode denoise lifts SSIM-vs-clean by +0.010–0.013 at cq20/26 while using fewer bits, and the motion-compensated warp (`mc=1`) edges same-coordinate by a consistent +0.0009 SSIM at the high-motion points with no regression (the confidence gate down-weights noise-matched MVs). Two committed RD-curve graphs (`docs/development/images/`, regenerable via the new `scripts/bench/plot_rd.py`). Documents the metric pathology that blocks a single BD-rate figure here: VMAF saturates (~99.8, noise-tolerant) and SSIM-vs-clean inverts the RD curve for denoise-vs-clean — SSIMULACRA2 is the tracked follow-up.
