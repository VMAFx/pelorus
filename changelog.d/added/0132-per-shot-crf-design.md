- **Per-shot CRF steering design (ADR-0132)**: scoped the biggest remaining BD-rate lever — per-shot complexity-budget CRF. Design: aggregate the signals analyze (variance/edge/banding) and mc (motion magnitude/entropy) already produce into a per-frame complexity scalar (EMA smoothed within shots, reset on `mc.has_scene_cut`), apply it as a per-frame uniform qoffset on the **already-proven** `AV_FRAME_DATA_REGIONS_OF_INTEREST` channel (summed with the per-tile auto-ROI — no new encoder patch), and **learn** the complexity→qoffset mapping with the ADR-0106 autotune loop gated on `honored_fraction` (so it ships safe-by-construction, off until a positive multi-metric BD-rate is proven). Smallest-first build order documented; no code yet (design record).
