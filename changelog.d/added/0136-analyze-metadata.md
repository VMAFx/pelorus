- **`vf_pelorus_analyze_vulkan`** now also emits its per-frame scalars as FFmpeg frame metadata under the `lavfi.pelorus.*` namespace (`complexity`, `texture`, `motion`, `variance`, `edge`, `banding`, `scene_cut`) — the `vf_scdet` idiom ([ADR-0136](docs/adr/0136-analyze-frame-metadata.md)). The interop blob isn't muxed to file, so this is the host-readable extraction path for per-shot CRF steering, the autotune loop, and shell debugging (`ffprobe -show_frames` / `metadata=print`). Additive and host-side: no interop-ABI or shader change. Docs: `docs/metrics/analyze.md`.
