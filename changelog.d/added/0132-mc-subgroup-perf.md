- **`vf_pelorus_mc` subgroup SAD reduction (~5% faster) + GPU throughput profiling**: profiled all filters on the RTX 4090 (720p/64f) — mc is the bottleneck at ~4× the others because block-matching SADs every candidate. Replaced its 32×32 shared-memory barrier-tree in `block_sad` with a two-level subgroup reduction (`subgroupAdd` + lane-0 partial combine, `GL_KHR_shader_subgroup_{basic,arithmetic}`), measured **~5% faster** (0.765→0.727 s warm) with bit-near-identical output (tree-vs-subgroup SSIM 0.9998 — no argmin change). `pelorus_mc.comp` in lockstep. `docs/development/bench-results.md` v0.4 documents the profiling + the honest finding that the bottleneck is per-candidate memory fetches (the real headroom is shared-memory reference caching, a tracked follow-up), so the broader "subgroup/fp16 free-throughput" item is narrower than proposed; analyze/grain (already fast) and fp16 (fetch-bound, 8-bit storage) were deliberately not pursued. Profiling graph committed.
