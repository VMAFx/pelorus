# Ordered list of Pelorus patches against FFmpeg n8.1.1.
# Applied in order by ffmpeg-patches/test/build-and-run.sh, and regenerated
# from files/ by ffmpeg-patches/generate.sh. Apply with `git am --3way` onto a
# pristine n8.1.1 checkout (see README.md). The stack is cumulative; verify
# with a full series replay, never per-patch `git apply --check`.
#
# Each fork-local patch links libpelorus (>= 0.1.0, pkg-config) and is gated by
# `vulkan spirv_library` so it only builds where the Vulkan compute path exists.
#
# Roadmap (not yet generated; sources land under files/ first):
#   00NN-*-fgs-bsf.patch                        FGS OBU/SEI bitstream filter (consumes 0006's params)
#   00NN-nvenc-pelorus-me-hints.patch           NVENC external ME hints (consumes 0007's MV field)
#
0001-add-vf_pelorus_deband_vulkan.patch
0002-add-vf_pelorus_analyze_vulkan.patch
0003-add-vf_pelorus_denoise_vulkan.patch
0004-nvenc-pelorus-roi.patch
0005-qsv-pelorus-roi.patch
0006-add-vf_pelorus_grain_estimate_vulkan.patch
0007-add-vf_pelorus_mc_vulkan.patch
